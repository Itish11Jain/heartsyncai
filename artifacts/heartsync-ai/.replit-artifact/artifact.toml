kind = "web"
previewPath = "/"
title = "HeartSync AI"
version = "1.0.0"
id = "artifacts/heartsync-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18373

[services.development]
run = "pnpm --filter @workspace/heartsync-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/heartsync-ai", "run", "build" ]
publicDir = "artifacts/heartsync-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18373"
BASE_PATH = "/"
